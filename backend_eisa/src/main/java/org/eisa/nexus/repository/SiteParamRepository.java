package org.eisa.nexus.repository;

import org.eisa.nexus.entity.SiteParam;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SiteParamRepository extends JpaRepository<SiteParam, String> {
}
