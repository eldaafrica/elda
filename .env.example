# PostgreSQL — utilisé par Spring Boot ET par le container db
SPRING_DATASOURCE_USERNAME=eisa_nexus
SPRING_DATASOURCE_PASSWORD=CHANGE_ME_strong_password

# JWT secret (base64, 64+ caractères)
JWT_SECRET=CHANGE_ME_base64_encoded_secret_at_least_64_characters_long_here

# Compte admin créé au premier démarrage
ADMIN_EMAIL=admin@elda-africa.org
ADMIN_PASSWORD=CHANGE_ME_strong_password
